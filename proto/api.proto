syntax = "proto3";
option go_package = "./warehousepb";

import "auth.proto";

message Request {
  oneof data {
    AuthRequest auth = 1;
  }
}

message Response {
  oneof data {
    AuthResponse auth = 2;
  }
}

service Auth {
  rpc Authenticate(AuthRequest) returns (AuthResponse);
}
